## IDF Component Manager Manifest File
dependencies:
  espressif/led_strip:
    version: ^3
    rules:
      - if: 'target in ["esp32s3"]'
  espressif/usb_host_cdc_acm:
    version: "2.*"
    rules:
      - if: 'target in ["esp32s3"]'
